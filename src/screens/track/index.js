import React from 'react';
import {View, StyleSheet} from 'react-native';
import AppText from '../../components/UI/AppText';

const TrackOrder = () => {
  return (
    <View>
      <AppText>welcome to tracking</AppText>
    </View>
  );
};

const styles = StyleSheet.create({});

export default TrackOrder;
