import React from 'react';
import {View, StyleSheet} from 'react-native';
import AppText from '../../components/UI/AppText/AppText';

const Cart = () => {
  return (
    <View>
      <AppText>welcome to cart screen</AppText>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Cart;
