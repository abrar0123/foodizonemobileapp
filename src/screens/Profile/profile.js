import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const Profile = () => {
  return (
    <View>
      <Text>welcome in profile</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Profile;
